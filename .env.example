# Copy to .env (gitignored). Loaded by ahd.settings.Settings via pydantic-settings.
# Never commit real values; never print them.
DEEPSEEK_API_KEY=
HF_TOKEN=
SERPER_API_KEY=
# (Serper_key is accepted as an alias for SERPER_API_KEY)
